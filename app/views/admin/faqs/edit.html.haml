- content_for :page_title do
  = t('admin.faqs.edit')

= simple_form_for @faq, url: admin_faq_path(@faq) do |form|
  = render 'shared/error_messages', object: @faq

  = render form

  .actions
    = form.button :button, t('generic.save_changes'), type: :submit
