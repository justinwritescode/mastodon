#!/usr/bin/with-contenv bash
# shellcheck shell=bash

export RAILS_ENV=production
export PATH="/mastodon/bin:${PATH}"

cd /opt/mastodon && \
bash bin/display-branding && \
bash bin/start-sshd && \
bash bin/start-nginx && \
echo "**** Starting streaming ****" && \
node ./streaming/index.js;
